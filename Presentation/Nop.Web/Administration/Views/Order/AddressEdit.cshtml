@model OrderAddressModel

@{
    //page title
    ViewBag.Title = T("Admin.Pedidos.Address.EditAddress").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Pedidos");
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Pedidos.Address.EditAddress") 
            <small><i class="fa fa-arrow-circle-left"></i>@Html.ActionLink(T("Admin.Pedidos.Address.BackToOrder").Text, "Edit", new { id = Model.OrderId })</small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdateAddress", Model)
}
